# Base de données SQLite (chemin relatif au dossier packages/server)
DATABASE_URL="file:./dev.db"

# Port du serveur backend
PORT=3001

# URL du frontend (pour CORS) — optionnel en développement local
# CLIENT_URL="http://localhost:5173"
